<div>

  <% if @students.empty? %>
  No current students
  <% else %>
  <% @students.each do |student| %>
    <div><%= link_to(student.to_s, student_path(student.id)) %></div>
  <% end %>
  <% end %>
</div>